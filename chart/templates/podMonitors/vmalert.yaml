apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: vmalert
spec:
  selector:
    matchLabels:
      app.kubernetes.io/instance: prom-bench
      job: vmalert
  podMetricsEndpoints:
  - port: metrics